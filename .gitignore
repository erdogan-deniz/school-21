# ── Общий блок — одинаков во всех Claude-проектах Дениза ──
# Правишь его — правь во всех сразу. Список проектов и правило синхронизации
# — в project-manager/README.md (с 12.09.2026; раньше life/CLAUDE.md §5). Правила самого проекта — ниже, после блока.

# Служебная авто-история плагина remember: генерируется сама, руками не правим.
.remember/

# Артефакты Claude Code и плагинов: локальные worktree, личные настройки сессии,
# логи браузерной автоматизации, состояние brainstorming-компаньона.
.claude/worktrees/
.claude/settings.local.json
.playwright-mcp/
.superpowers/

# Аварийные дампы Git Bash / MSYS — появляются в корне сами, содержимого не несут.
# Дважды попадали в историю через `git add -A`: life 21.08.2026, ai-agency 07.09.2026.
*.stackdump

# Системный мусор Windows / macOS, временные файлы редакторов и Office.
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store
*.tmp
*.bak
*~
~$*
nul
# ── конец общего блока ──

# --- Operating system noise ---
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride

# --- Editor / IDE scratch ---
.vscode/
.idea/
*.iml
*.swp
*.swo
*~
.project
.cproject
.settings/
.classpath

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Python virtual environments (any name; "charisel" is the user's pattern)
env/
.env/
venv/
.venv/
ENV/
env.bak/
venv.bak/
charisel/
.charisel/

# pytest / coverage / linters
.pytest_cache/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
.cache/
nosetests.xml
coverage.xml
*.cover
*.lcov
.ruff_cache/
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/

# Jupyter / IPython
.ipynb_checkpoints/
profile_default/
ipython_config.py

# --- C / C++ build artefacts ---
*.o
*.obj
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll
*.exe
*.out
*.app
*.gch
*.pch
*.gcno
*.gcda
*.gcov
*.profraw
*.profdata
gmon.out
report/
build_files/
cmake-build-*/

# Qt / qmake leftovers
moc_*.cpp
moc_*.h
qrc_*.cpp
ui_*.h
*.user
*.qm

# --- Documentation builds ---
docs/_build/
docs/html/
docs/latex/
# Doxygen output of every library subproject (OUTPUT_DIRECTORY = docs/api)
**/docs/api/
*.aux
*.toc
*.synctex.gz

# --- Coverage and reports (generic) ---
coverage/
*.lcov

# --- Claude Code internal artefacts ---
# The superpowers plugin caches itself wherever it's invoked from.
**/superpowers/
.claude/

# --- Misc ---
*.log
*.tmp
*.bak
node_modules/
