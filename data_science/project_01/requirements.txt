nltk>=3.8,<4
spacy>=3.7,<4
gensim>=4.3,<5
pandas>=2.0,<3
ipykernel>=6.29,<7
symspellpy>=6.7,<7
scikit-learn>=1.3,<2
# scipy 1.10.1 was hard-pinned, but that release dropped wheels for
# Python 3.12. Relax to a range that still covers the 1.10 API the
# notebook depends on and lets new Python versions resolve cleanly.
scipy>=1.11,<2
