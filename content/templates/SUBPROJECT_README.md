<!--
  Subproject README template for the `school-21` repo.

  Production fold goes ON TOP. Original School 21 task description is
  preserved verbatim BELOW the `---` divider.

  Replace every `<placeholder>` and remove this comment block before commit.
-->

# `<Subproject Name>`

[![CI](https://github.com/<owner>/<repo>/actions/workflows/<language>.yml/badge.svg?branch=main)](https://github.com/<owner>/<repo>/actions/workflows/<language>.yml)
[![Coverage](https://img.shields.io/badge/coverage-<NN>%25-<color>.svg)](#tests)
[![Docs](https://img.shields.io/badge/docs-doxygen-blue.svg)](docs/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](../../LICENSE)

> *<One- or two-sentence tagline describing what this subproject is and why it exists.>*

## Quick start

```bash
# Build
<one-liner: make / docker build -t <name> . / pip install -e . / npm install>

# Run
<one-liner: ./bin/<name> / docker run --rm <name> / python -m <module>>

# Test
<one-liner: make test / pytest / docker run --rm <name> test>
```

For a fully reproducible environment:

```bash
docker build -t school21/<subproject> .
docker run --rm -it school21/<subproject>
```

## Demo

![demo](misc/demo.gif)

<!-- For CLI projects: an asciinema cast or a screen-record gif.
     For GUI projects: a screenshot or short capture.
     Required by DoD item G. -->

## Documentation

- API reference: [`docs/`](docs/) (generated by Doxygen / Sphinx — `make docs`)
- Coverage report: [`coverage/`](coverage/) (regenerated by `make coverage` and CI)
- Architectural notes: [`docs/architecture.md`](docs/architecture.md) *(if present)*

## Tests

- Framework: *<Check / GoogleTest / pytest / sqlfluff / bats>*
- How to run: see *Quick start → Test*.
- Current coverage: ![coverage](https://img.shields.io/badge/coverage-<NN>%25-<color>.svg)

## License & attribution

This project was developed as part of the **School 21** curriculum (analogue of School 42).

The repository as a whole is licensed under the **MIT License** — see the root [`LICENSE`](../../LICENSE).
The `LICENSE` file inside this subproject (`# School 21 License`) is preserved as educational attribution and historical artefact; it does **not** override the repo-wide MIT licence.

---

## Original task (School 21)

<!--
  Below this divider, paste the existing School 21 task content verbatim:
  Preamble (story), Chapter I/II/III headings, Information sections,
  Part 1 / Part 2 requirement lists, etc. Do NOT trim or paraphrase.

  Use H2 ("##") for the divider heading — markdownlint MD025 forbids
  multiple H1s in the same document. The horizontal rule above plus the
  H2 already produces a clear visual break.
-->
